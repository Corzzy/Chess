using UnityEngine;

public class Grid : MonoBehaviour
{
    public GameObject[] tiles;

    public Vector2 FindVector(Vector2 mousePos)
    {
        Transform closestTile;
        float closestDistance = 0;
        for(int i = 0; i < tiles.Length; i++)
        {
            
        }
    }
}
